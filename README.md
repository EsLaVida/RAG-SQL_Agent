# 🤖 AI SQL Analyst (LangGraph + Xiaomi MiMo-V2)

Интеллектуальный SQL-агент нового поколения, способный к самокоррекции и глубокому анализу баз данных. В отличие от простых RAG-систем, проект использует **многоагентную архитектуру** (Multi-Agent Workflow), что позволяет системе «думать» и исправлять логические ошибки до того, как запрос увидит пользователь.

## 🧠 Эволюция проекта: От Mistral к MiMo-V2
В ходе разработки было проведено исследование эффективности моделей для задач SQL-агентов:
* **Mistral 7B/8B (Rejected):** Модель показала слабую стабильность в многошаговых диалогах. Часто возникали ошибки "Role Collision" (конфликт ролей) и галлюцинации в сложных SQL-связях (JOIN).
* **Xiaomi MiMo-V2-Flash (Selected):** Выбрана как основной "мозг" системы через OpenRouter. Обладает 309B параметрами (MoE) и феноменальной способностью к рассуждению (Reasoning). Она не просто пишет код, а понимает бизнес-логику запроса.

## ✨ Ключевые особенности

* **🛡️ Self-Correction Loop**: Если внутренний валидатор находит ошибку или видит риск потери данных, он отправляет фидбек агенту. Агент вежливо признает ошибку и корректирует SQL (например, заменяет `JOIN` на `LEFT JOIN`).
* **🤝 Human-in-the-loop**: Никакой магии за спиной. Агент выполняет запрос только после вашего явного подтверждения.
* **🧩 Динамическое изучение схемы**: Агент самостоятельно исследует структуру таблиц, индексы и связи, адаптируясь к любой SQLite базе в реальном времени.
* **🚀 Высокая скорость**: Благодаря архитектуре MiMo-V2-Flash, генерация и проверка даже сложных запросов занимает считанные секунды.

## 🛠 Технологический стек

* **LangGraph**: Управление сложным графом состояний и цикличной логикой агента.
* **Xiaomi MiMo-V2-Flash**: SOTA-модель (через OpenRouter) для глубокой логики и кодинга.
* **LangChain**: Фреймворк для интеграции инструментов и работы с LLM.
* **SQLite**: Локальное хранилище данных.

## 📊 Архитектура графа
Проект реализует следующий цикл:
1. **Input**: Вопрос пользователя на естественном языке.
2. **Validator**: Проверка синтаксиса и безопасности (только SELECT).
3. **Optimizer**: Анализ логики (проверка связей и полноты данных).
4. **Agent Interaction**: Если найдены замечания, агент исправляет запрос, имитируя интерактивный диалог.
5. **Output**: Визуализация данных и инсайты.


## 🚀 Быстрый старт

### 1. Подготовка окружения
Клонируйте репозиторий и установите зависимости:
```bash
python -m venv venv
source venv/bin/activate  # Для Windows: venv\Scripts\activate
pip install -r requirements.txt


### 2. Запуск агента
Запустите основной скрипт:
```bash
python main.py
```
Введите запрос на естественном языке, например:
- "Сколько сотрудников в компании?"
- "Покажи всех менеджеров"
- "Какие у сотрудников навыки?"

Агент сам изучит схему и выполнит SQL-запрос с подтверждением.

## 📋 Примеры запросов

- "Сколько всего сотрудников в компании?"
- "Покажи имена и должности менеджеров"
- "Какие навыки у сотрудников из отдела IT?"
- "Выведи список всех проектов и их статусы"

## 📊 Пример вывода

После подтверждения агент выполнит SQL-запрос и отобразит данные в удобном формате


## 🤝 Взаимодействие с пользователем (Human-in-the-loop)

Проект реализует концепцию **безопасного выполнения кода**. Агент не является автономным исполнителем в вакууме — он работает в тесном контакте с пользователем:

1. **Прозрачность**: Перед выполнением любого действия агент выводит сгенерированный SQL-запрос на экран.
2. **Контроль**: Система ожидает явного подтверждения (`y/n`) от пользователя.
3. **Обратная связь**: Если пользователь отклоняет запрос или просит внести правки, агент пересчитывает логику, учитывая новые комментарии.

Это гарантирует, что даже в случае сложных аналитических задач пользователь всегда сохраняет контроль над операциями с данными.
# 🤖 AI SQL Analyst (LangGraph RAG-SQL)

Интеллектуальный агент для работы с базами данных SQLite на основе естественного языка. Проект построен на архитектуре **LangGraph**, что позволяет агенту самостоятельно изучать схему данных, составлять SQL-запросы и исправлять собственные ошибки.

## ✨ Ключевые особенности

* **Human-in-the-loop (Guardrails)**: Агент никогда не выполнит запрос без явного подтверждения пользователя.
* **Динамическое изучение схемы**: Агент использует инструмент `get_db_schema`, чтобы адаптироваться к структуре таблиц в реальном времени.
* **Безопасность**: Доступны только `SELECT` запросы. Попытки модификации данных блокируются на уровне инструментов.
* **Самокоррекция**: Если SQL-запрос вызывает ошибку, агент анализирует её и предлагает исправленный вариант.

## 🛠 Технологический стек

* **Python 3.10+**
* **LangChain / LangGraph**: Управление логикой и графом состояний.
* **LLM**: OpenAI GPT-4o / GPT-4o-mini (или аналоги через LangChain).
* **SQLite**: Локальная база данных.

## 🚀 Быстрый старт

### 1. Подготовка окружения
Клонируйте репозиторий и установите зависимости:
```bash
python -m venv venv
source venv/bin/activate  # Для Windows: venv\Scripts\activate
pip install -r requirements.txt


### 2. Запуск агента
Запустите основной скрипт:
```bash
python main.py
```
Введите запрос на естественном языке, например:
- "Сколько сотрудников в компании?"
- "Покажи всех менеджеров"
- "Какие у сотрудников навыки?"

Агент сам изучит схему и выполнит SQL-запрос с подтверждением.

## 📋 Примеры запросов

- "Сколько всего сотрудников в компании?"
- "Покажи имена и должности менеджеров"
- "Какие навыки у сотрудников из отдела IT?"
- "Выведи список всех проектов и их статусы"

## 📊 Пример вывода

После подтверждения агент выполнит SQL-запрос и отобразит данные в удобном формате


## 🤝 Взаимодействие с пользователем

Агент будет запрашивать у пользователя подтверждение перед выполнением каждого SQL-запроса, чтобы обеспечить прозрачность и контроль над операциями с данными.


